generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Swapnil — Add User and PasswordReset models below this line


// Rishima — Add Listing model below this line
